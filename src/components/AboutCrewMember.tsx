import React from "react";

export interface AboutCrewMemberProps {
  name: string;
  imgSrc?: string;
  description: string;
  role: string;
}

export const AboutCrewMember = ({
  name,
  description,
  role,
}: AboutCrewMemberProps) => {
  return;
};
